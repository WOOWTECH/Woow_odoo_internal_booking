<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Menu Structure:
        Calendar (calendar.menu_calendar_myopic)
        ├── Meetings (existing)
        ├── Resource Bookings (new)
        └── Configuration
            ├── Meeting Types (existing)
            └── Booking Resources (new)
    -->

    <!-- Main Menu Item under Calendar -->
    <menuitem id="menu_booking_reservation"
              name="Resource Bookings"
              parent="calendar.menu_calendar_myopic"
              action="action_booking_reservation_calendar"
              sequence="20"/>

    <!-- Configuration Menu (under Calendar > Configuration) -->
    <menuitem id="menu_booking_resource_type"
              name="Booking Resources"
              parent="calendar.menu_calendar_config"
              action="action_booking_resource_type"
              sequence="20"/>
</odoo>
